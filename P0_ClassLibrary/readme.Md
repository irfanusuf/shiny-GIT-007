dotnet add package Microsoft.Extensions.Options     // not avialable in .net class library by default

dotnet add package Microsoft.http.abstractions     // not avialable in .net class library by default 